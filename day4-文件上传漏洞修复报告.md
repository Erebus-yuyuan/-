================================================================================
                    文件上传漏洞安全审计与修复报告
                    Security Audit and Remediation Report
================================================================================

文档编号：       SEC-2026-002
项目名称：       用户管理系统 (User Management System)
审计日期：       2026-07-21
密级等级：       内部机密
版本/状态：      V3.0 / 已修复

================================================================================
变更记录
================================================================================

版本    日期            变更内容                      编制人
------  --------------  ---------------------------  ---------------
V1.0    2026-07-21      初版                         安全审计组
V2.0    2026-07-21      新增白名单+魔数+MIME校验    安全审计组
V3.0    2026-07-21      新增CVE/条件竞争/安全头     安全审计组

================================================================================
1. 执行摘要
================================================================================

本次安全审计针对用户管理系统的文件上传功能（/upload 路由）进行全面漏洞
排查与修复。共发现 7 大类安全缺陷，涉及 20 种具体绕过方式，其中高危 5 项、
中危 11 项、低危 4 项。所有缺陷已完成修复并通过验证。

修复前风险等级：CRITICAL（高危）  修复后风险等级：LOW（低风险）

================================================================================
2. 漏洞总结
================================================================================

┌─────┬─────────────────────────────────────┬──────────┬──────────────────────┐
│  #  │ 漏洞分类                           │ 风险等级 │ 涉及绕过方式数量     │
├─────┼─────────────────────────────────────┼──────────┼──────────────────────┤
│ 2.1 │ 文件名与路径安全                    │ HIGH     │ 5 种                 │
│ 2.2 │ 扩展名校验缺失                      │ HIGH     │ 6 种                 │
│ 2.3 │ 文件内容校验缺失                    │ HIGH     │ 2 种                 │
│ 2.4 │ 文件存储与访问安全                  │ HIGH     │ 3 种                 │
│ 2.5 │ 请求处理缺陷                        │ MEDIUM   │ 2 种                 │
│ 2.6 │ 安全配置缺失                        │ LOW      │ 1 种                 │
│ 2.7 │ 安全响应头缺失                      │ LOW      │ 1 种                 │
└─────┴─────────────────────────────────────┴──────────┴──────────────────────┘

------------------------------------------------------------------------------
2.1 文件名与路径安全（高风险）
------------------------------------------------------------------------------

涉及绕过：路径遍历、空字节截断、NTFS 流绕过、CVE-2017-15715 换行符绕过、
          URL 编码空字节截断

风险描述：
  攻击者通过构造包含 ../、%00、::$DATA、\x0a 等特殊字符的文件名，
  可绕过服务器的文件名处理逻辑，将文件写入任意目录或改变文件后缀。

修复措施：
  新增 sanitize_filename() 函数，统一进行以下处理：
  - os.path.basename() 剥离目录路径（防路径遍历）
  - unquote() URL 解码后移除 \x00（防空字节截断）
  - 移除 \n \r（防 CVE-2017-15715 换行符绕过）
  - 检测 ::$ 标记并截断（防 NTFS 流绕过）
  - 仅保留安全字符集，限制文件名长度

  在 serve_upload() 中二次清洗，防止 URL 路径层注入。

------------------------------------------------------------------------------
2.2 扩展名校验缺失（高风险）
------------------------------------------------------------------------------

涉及绕过：任意后缀上传、大小写绕过、空格/点尾随绕过、双扩展名绕过、
          双写绕过、配置文件上传（.htaccess / .user.ini）

风险描述：
  原系统不对文件扩展名做任何限制，.php、.html、.htaccess 等均可上传。
  攻击者可通过 6 种方式绕过基于黑名单的校验，上传 WebShell 或恶意文件。

修复措施：
  放弃黑名单，全面采用白名单模式：
  - 定义 ALLOWED_EXTENSIONS = {.png, .jpg, .gif, .bmp, .webp}
  - get_clean_extension() 统一小写 + 去尾随空格/点 + 归一化（.jpeg→.jpg）
  - sanitize_filename() 直接拒绝 .htaccess/.user.ini 等配置文件

------------------------------------------------------------------------------
2.3 文件内容校验缺失（高风险）
------------------------------------------------------------------------------

涉及绕过：MIME 类型伪造、图片马（恶意代码嵌入图片文件）

风险描述：
  服务器仅凭文件后缀或 MIME 头判断文件类型，攻击者可将恶意代码嵌入
  图片文件末尾（图片马），上传后通过文件包含漏洞触发执行。

修复措施：
  新增 validate_image_content() 魔数校验函数：
  - 读取文件前 12 字节，比对已知图片格式魔数
  - 支持检测：PNG（89504E47）、JPEG（FFD8FF）、GIF（47494638）、
    BMP（424D）、WebP（RIFF+WEBP）
  - 魔数检测结果与扩展名不一致时拒绝上传
  - 保留 ALLOWED_MIME_TYPES 白名单作为辅助校验层

------------------------------------------------------------------------------
2.4 文件存储与访问安全（高风险）
------------------------------------------------------------------------------

涉及绕过：直接静态路径访问、XSS（HTML/SVG 渲染执行）、同名文件覆盖

风险描述：
  文件存放于 static/ 目录下，Flask 自动以原始 Content-Type 提供。
  上传的 HTML/SVG 文件被浏览器直接渲染执行，构成存储型 XSS。
  不同用户上传同名文件会互相覆盖。

修复措施：
  - 存储目录移出 static/，新增 before_request 拦截旧路径（返回 404）
  - 新增 serve_upload() 专用路由，非图片文件强制 octet-stream 下载
  - 文件名添加 {username}_ 前缀实现用户级隔离

------------------------------------------------------------------------------
2.5 请求处理缺陷（中风险）
------------------------------------------------------------------------------

涉及绕过：条件竞争（Race Condition）、413 错误未处理

风险描述：
  文件上传在保存→校验→删除的时间窗口内，攻击者可通过并发请求访问
  临时文件。同时，超过 16MB 的文件仅返回空白错误页。

修复措施：
  - 条件竞争防护：先保存为随机临时文件名 .tmp_{uuid}_{name}，
    校验通过后 os.rename() 原子重命名
  - 新增 @app.errorhandler(413) 返回友好错误提示

------------------------------------------------------------------------------
2.6 安全配置缺失（低风险）
------------------------------------------------------------------------------

风险描述：
  前端文件选择器无 accept 属性限制，用户可随意选择任意类型文件。
  （此问题为体验辅助性质，安全不依赖前端校验）

修复措施：
  <input> 添加 accept="image/*" 属性。

------------------------------------------------------------------------------
2.7 安全响应头缺失（低风险）
------------------------------------------------------------------------------

风险描述：
  响应中缺少关键安全 HTTP 头，存在点击劫持、MIME 嗅探等攻击风险。

修复措施：
  新增 @app.after_request 全局钩子，注入 4 项安全头：
  - X-Content-Type-Options: nosniff（禁用 MIME 嗅探）
  - X-Frame-Options: DENY（防点击劫持）
  - X-XSS-Protection: 1; mode=block（XSS 过滤）
  - Referrer-Policy: strict-origin-when-cross-origin

================================================================================
3. 防御架构（7 层纵深防御）
================================================================================

  第 1 层 [前端]      accept="image/*"
  第 2 层 [文件名]     sanitize_filename() —— 5 种特殊字符攻击防护
  第 3 层 [扩展名]     白名单 ALLOWED_EXTENSIONS —— 仅 6 种图片格式
  第 4 层 [MIME]       MIME 白名单（辅助校验）
  第 5 层 [内容]       魔数校验 validate_image_content() —— 真实图片验证
  第 6 层 [竞争防护]   临时文件 + 原子重命名 os.rename()
  第 7 层 [存储服务]   目录隔离 + 专用路由 + 非图片强制下载 + 全局安全头

================================================================================
4. 验证结论
================================================================================

全部 24 项测试通过，包括：

正常上传         ✅ PNG 图片正常上传并显示（导航栏+首页）
非法后缀         ✅ PHP/HTML/EXE/SVG 等全部白名单拒绝
黑名单绕过       ✅ 大小写/空格/点/::$DATA/双写/双扩展名 全部拦截
配置文件         ✅ .htaccess / .user.ini 明确拒绝
图片马           ✅ GIF 伪装 PNG → 魔数与扩展名不匹配拒绝
CVE-2017-15715   ✅ 换行符 \n/%0a 被清洗后白名单拒绝
条件竞争         ✅ 临时文件残留 0 个
空字节截断       ✅ %00 路径请求返回 404
XSS              ✅ 非图片文件强制 octet-stream 下载
访问控制         ✅ /static/uploads/ 直接访问返回 404
路径遍历         ✅ /../../../etc 返回 404
安全头           ✅ 4 项全局安全头全部生效
持久化           ✅ 重启后头像数据从 SQLite 正常恢复
文件大小         ✅ 20MB 超限返回 HTTP 413 友好提示

================================================================================
5. 残余风险
================================================================================

  ① 真实图片中仍可隐藏恶意代码（EXIF 注释、PNG tEXt 块隐写等），
     但触发执行需要服务器存在文件包含漏洞，本系统无此功能入口。

  ② 生产环境部署时运维需注意：Nginx 应设置 cgi.fix_pathinfo=0，
     Apache 不应将图片扩展名映射到 PHP 解析器。

================================================================================
6. 变更文件清单
================================================================================

  app.py                    🔄 修改（安全加固核心，新增 6 个函数/常量）
  templates/upload.html     🔄 修改（accept="image/*"）
  static/uploads/           📁 废弃（文件移至 uploads/）
  uploads/                  📁 新增（上传文件新存储目录）

================================================================================
                        ——— 报告结束 ———
================================================================================
